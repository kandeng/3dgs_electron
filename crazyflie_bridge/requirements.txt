# Crazyflie Bridge Dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Package descriptions:
#   cflib          — Crazyflie Python client library (drone control & telemetry)
#   websockets     — WebSocket client/server for gateway communication
#   aiortc         — WebRTC peer connection for video streaming
#   av             — PyAV, required by aiortc for video frame encoding
#   opencv-python  — OpenCV, used for camera capture and frame processing

cflib
websockets
aiortc
av
opencv-python
